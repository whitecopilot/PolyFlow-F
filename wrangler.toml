name = "polyflow-front"
compatibility_date = "2024-12-01"

# 静态资源配置
# not_found_handling: SPA 路由支持，未匹配的路由返回 index.html
[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# 注意: VITE_* 环境变量需要在构建时设置，不是运行时
# 构建命令中已通过 WORKERS_CI_BRANCH 判断并设置 VITE_CHAIN_ENV 和 VITE_API_BASE_URL
